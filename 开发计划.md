# 《星屿》开发计划 — 纯前端版

## 文档信息

| 项目 | 内容 |
| :--- | :--- |
| 文档名称 | 《星屿》开发计划 |
| 版本号 | v2.0（纯前端版） |
| 更新日期 | 2026年8月6日 |
| 关联文档 | `PROJECT_PLAN.md`（技术计划书）、`大白话需求.md`（需求依据） |
| 技术方案 | **纯前端 + IndexedDB（无后端、无云端）** |
| 开发模式 | 迭代开发，分里程碑交付 |

> 说明：本版本取代早期基于「Node 后端 + JSON 文件」的 v1.0 开发计划。第一版落地方式已确定为纯前端单页应用，数据全部存浏览器本地。

---

## 一、技术栈总览

| 技术 | 用途 |
| :--- | :--- |
| Vue 3 + Vite | 前端框架与构建 |
| Vue Router（hash 模式） | 5 个 Tab 路由切换 |
| Pinia | 状态管理（按模块拆 store） |
| Dexie.js | IndexedDB 本地数据库封装 |
| ECharts + echarts-wordcloud | 全部图表与关键词云 |
| 原生 CSS 变量 | 圆润柔和儿童风格样式 |
| 原生 SVG | 成长年轮树 |
| 原生 JS（Date / setInterval） | 日历、学习计时器 |
| html2canvas | 导出汇总长图 |

---

## 二、总体开发路线图

```
M0: 项目骨架 ─────────────── Vite + 路由 + Pinia + Dexie + 样式 + TabBar
    │
M1: 数据层 + 种子数据 ──────── 建表、种子数据、工具函数
    │
M2: 客厅（首页） ───────────── 最高优先级，打开即见
    │
M3: 点滴 ───────────────────── 时间线 + 身体数据
    │
M4: 知识库 ─────────────────── 学科 + 常识 + 技能
    │
M5: 通讯录 ─────────────────── 联系人 + 关系网
    │
M6: 我的 ───────────────────── 周报 + 年轮树 + 管理员模式 + 导出
    │
M7: 提醒 + 集成验证 ─────────── 睡前提醒、边界处理、验收
```

---

## 三、里程碑详细任务

### M0：项目骨架

**目标**：搭建可运行的空壳应用，5 个 Tab 能切换。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 初始化项目 | `package.json`、`vite.config.js`、`index.html` | `npm install` 成功，`npm run dev` 可启动 |
| 2 | 安装依赖 | — | vue、vue-router、pinia、dexie、echarts、echarts-wordcloud、html2canvas 安装成功 |
| 3 | 路由配置 | `src/router/index.js` | 5 个路由（/living-room /diary /knowledge /contacts /profile），hash 模式 |
| 4 | 入口与根组件 | `src/main.js`、`src/App.vue` | 挂载 Vue + Pinia + Router |
| 5 | 底部导航 | `src/components/TabBar.vue` | 5 个 Tab 图标+文字，点击切换，当前项高亮 |
| 6 | 空页面占位 | `src/views/*.vue`（5 个） | 每个 Tab 显示占位标题 |
| 7 | 样式基础 | `src/styles/variables.css`、`global.css` | CSS 变量定义配色/圆角，移动端优先 |

**验收**：浏览器打开能看 5 个 Tab，点击可切换，URL 正确变化。

---

### M1：数据层 + 种子数据

**目标**：建立本地数据库、写入种子数据、提供聚合工具函数。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 数据库定义 | `src/db/index.js` | Dexie 建 6 张表（moods/health/diary/knowledge/contacts/config） |
| 2 | 种子数据初始化 | `src/db/index.js`（populate 钩子） | 首次打开自动写入：家人 3 人、每日一问 30 题、冷知识 10 条、知识树一个章节、悄悄话、默认配置 |
| 3 | 日期工具 | `src/utils/date.js` | 提供日历网格、周/月范围、去年今日等函数 |
| 4 | 聚合统计工具 | `src/utils/stats.js` | 提供连续打卡天数、平均心情、健康评分、学习时长、掌握度等计算 |
| 5 | Pinia store 骨架 | `src/stores/*.js`（6 个） | 每个 store 能 init（从 Dexie 加载）并暴露基础读写方法 |

**验收**：首次打开后 DevTools 里 IndexedDB 出现 `xingyu` 库及 6 张表，含种子数据；刷新数据不丢。

---

### M2：客厅（首页）

**目标**：实现首页所有模块的真实数据展示和交互。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 今日状态卡片 | `src/views/LivingRoom.vue` | 显示心情表情、一句话总结、连续打卡天数、三核心数字 |
| 2 | 情绪彩虹月历 | `src/components/MoodCalendar.vue` | 当月日历，每天显示心情 emoji，背景色按分值渐变，点击看详情 |
| 3 | 本周情绪趋势 | `src/components/TrendChart.vue` | 7 天折线图，X=周几，Y=心情值 |
| 4 | 四叶草健康环 | `src/components/CloverHealth.vue` | 四瓣环形进度（水/睡/餐/玩），全满触发发光动画 |
| 5 | 学科气泡图 | `src/components/SubjectBubbles.vue` | 3 个学科气泡，大小=学习时长，点击跳知识库 |
| 6 | 本周学习趋势 | `src/components/TrendChart.vue`（复用/新建） | 柱状图按学科堆叠，X=周几，Y=时长 |
| 7 | 每日一问 & 冷知识 | `src/views/LivingRoom.vue` 内 section | 按日期轮替取题，回答存 diary，收藏存 knowledge |
| 8 | 最近动态 | `src/views/LivingRoom.vue` 内 section | 跨模块聚合最近 5 条，按时间倒序 |
| 9 | 快捷打卡入口 | `src/components/QuickActions.vue` + `pickers/*` | 6 个悬浮按钮，喝水直接累加，其余弹对应选择器 |

**验收**：打卡心情/喝水后，首页卡片与图表实时更新，数据持久化。

---

### M3：点滴

**目标**：实现时间线记录和身体数据管理。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 时光时间线 | `src/views/Diary.vue` | 按日期倒序分组，不同类型左侧边框不同色，里程碑日期显示 🏆 |
| 2 | 身体成长曲线 | `src/views/Diary.vue` | 身高/体重/视力折线图，带数据点，可新增记录 |
| 3 | 写点滴面板 | `src/views/Diary.vue` 内嵌面板 | 4 种类型（见闻/灵感/技能/里程碑），保存后时间线最前端出现新记录 |

**验收**：新增点滴后时间线实时更新，身体数据新增后曲线多一个点。

---

### M4：知识库

**目标**：实现学科学习、常识科普、技能学习三大分类。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 三大分类切换 | `src/views/Knowledge.vue` | 学科学习/常识科普/技能学习 Tab 切换 |
| 2 | 学科进度卡片 | `src/views/Knowledge.vue` | 3 学科显示已点亮/总数进度条与百分比 |
| 3 | 知识树 | `src/views/Knowledge.vue`（递归组件） | 章节→知识点→星星，点击星星切换 4 级掌握度，刷新保持 |
| 4 | 学习日志 | `src/views/Knowledge.vue` | 列表 + 表单（学科/时长/内容/收获），保存后客厅学习趋势更新 |
| 5 | 学习计时器 | `src/views/Knowledge.vue`（原生 setInterval） | 开始/暂停/停止/手动调整，结束写入日志 |
| 6 | 冷知识收藏夹 | `src/views/Knowledge.vue` | 显示所有收藏的冷知识，空状态提示 |
| 7 | 技能清单 | `src/views/Knowledge.vue` | 聚合点滴 `type=skill` + 手动新增技能 |

**验收**：点星星后学科进度更新；计时结束生成学习日志；收藏的冷知识出现在收藏夹。

---

### M5：通讯录

**目标**：实现联系人管理和互动记录。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 标签分组列表 | `src/views/Contacts.vue` | 家人/亲戚/朋友/自定义标签，仿微信列表，切换正确筛选 |
| 2 | 添加联系人 | `src/views/Contacts.vue` | 姓名 + emoji 头像选择 + 标签，保存后出现在对应分组 |
| 3 | 联系人详情 | `src/views/Contacts.vue` | 个人信息/健康/心情/趣事分区卡片，底部操作栏 |
| 4 | 互动记录 | `src/views/Contacts.vue` 内嵌面板 | 记录健康/心情/趣事，保存后详情页对应卡片更新 |
| 5 | 悄悄话 | `src/views/Contacts.vue`（「我」详情） | 哥哥管理员模式下可编辑，妹妹只读 |
| 6 | 关系网可视化 | `src/components/RelationGraph.vue` | ECharts 力导向图，妹妹中心 + 最近互动 3 人连线 |

**验收**：新增联系人/互动记录后列表与关系网更新。

---

### M6：我的

**目标**：实现周报、成长可视化、管理员模式与导出。

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 本周周报 | `src/views/Profile.vue` | 显示平均心情、学习时长、打卡率、新增知识，数据与记录一致 |
| 2 | 关键词云 | `src/views/Profile.vue`（echarts-wordcloud） | 统计本周高频词，大小颜色按词频 |
| 3 | 成长年轮树 | `src/components/GrowthTree.vue`（SVG） | 果实=身高条数，年轮=里程碑数，数据变化树变化 |
| 4 | 管理员模式 | `src/views/Profile.vue` 入口 | 输哥哥密码进入，能录物料（冷知识/知识树/悄悄话/家人）+ 看汇总 |
| 5 | 导出汇总 | `src/utils/export.js` | 导出汇总 JSON + html2canvas 长图，**不含日记原文** |

**验收**：周报数据与实际一致；管理员能录物料看汇总但看不到日记原文；导出内容不含原文。

---

### M7：提醒 + 集成验证

| 序号 | 任务 | 文件 | 验收标准 |
| :--- | :--- | :--- | :--- |
| 1 | 睡前温柔提醒 | `src/App.vue` 或全局组件 | 晚上时段打开显示温柔提醒条，不强制 |
| 2 | 边界情况处理 | 全局 | 空状态引导、超长文本截断、防重复提交、刷新状态恢复 |
| 3 | 全流程验收 | — | 见下方验收清单逐项通过 |
| 4 | 移动端适配 | `src/styles/*` | 手机/平板竖屏正常显示，480px 宽度布局正常 |

---

## 四、验收清单（汇总）

| 类别 | 检查项 |
| :--- | :--- |
| 启动 | `npm install` / `npm run dev` 成功，浏览器打开首页 |
| 导航 | 5 Tab 显示、切换、高亮、客厅可滚动 |
| 功能 | 心情打卡、喝水打卡、写点滴、学习打卡+计时器、添加联系人、星星掌握、管理员模式、导出、收藏冷知识、关系网 |
| 数据 | 首次打开生成 6 张表含种子数据，刷新/重启数据不丢 |
| 视觉 | 色彩统一、响应式、动画流畅、字体清晰 |
| 兼容 | Chrome / Edge / Firefox 最新版 |

---

## 五、文件结构规范

```
src/
├── main.js
├── App.vue
├── router/index.js
├── stores/          # mood / health / diary / knowledge / contacts / config
├── db/index.js      # Dexie 数据库 + 种子数据
├── utils/           # stats.js / date.js / export.js
├── styles/          # variables.css / global.css
├── components/
│   ├── TabBar.vue
│   ├── MoodCalendar.vue
│   ├── CloverHealth.vue
│   ├── SubjectBubbles.vue
│   ├── TrendChart.vue
│   ├── GrowthTree.vue
│   ├── RelationGraph.vue
│   ├── QuickActions.vue
│   └── pickers/     # MoodPicker / MealPicker / SleepPicker / PlayPicker / StudyPicker
└── views/           # LivingRoom / Diary / Knowledge / Contacts / Profile
```

## 六、开发规范

| 项目 | 规范 |
| :--- | :--- |
| 组件命名 | PascalCase（如 `MoodCalendar.vue`） |
| Store 命名 | camelCase（如 `useMoodStore`） |
| 数据读写 | 统一通过 Pinia store 调用 Dexie，组件不直接碰 db |
| 样式 | 统一用 CSS 变量，不直接写颜色值 |
| 提交 | 每完成一个里程碑提交一次 |

---

## 七、时间估算（单人开发）

| 里程碑 | 预估时间 | 说明 |
| :--- | :--- | :--- |
| M0 项目骨架 | 0.5-1 天 | 空壳 + 导航 |
| M1 数据层 | 1 天 | 建表 + 种子数据 + 工具函数 |
| M2 客厅 | 2-3 天 | 图表最多，工作量最大 |
| M3 点滴 | 1 天 | 时间线 + 曲线 |
| M4 知识库 | 1-2 天 | 知识树 + 计时器 |
| M5 通讯录 | 1 天 | 联系人 + 关系网 |
| M6 我的 | 1-2 天 | 周报 + 年轮树 + 管理员/导出 |
| M7 集成 | 1 天 | 提醒 + 边界 + 验收 |
| **总计** | **8-11 天** | 纯前端，无后端开销 |

---

**文档结束**

本开发计划为纯前端版第一版可直接执行的路线图。每个里程碑都有明确的文件路径、任务目标和验收标准，开发过程中可按实际进展调整优先级。技术细节以 `PROJECT_PLAN.md` 为准。
